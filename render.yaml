services:
  - type: web
    name: mylocalpro
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/client
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
